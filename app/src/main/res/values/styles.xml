<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="CurrencyNowTheme" parent="android:Theme.Material" >
        <item name="android:colorPrimary">@color/mcgpalette0_900</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/mcgpalette0_500</item>
        <item name="android:textColorPrimary">@android:color/background_light</item>
        <item name="android:statusBarColor">@color/mcgpalette0_900</item>
        <item name="android:colorForeground">@android:color/background_light</item>
        <item name="android:navigationBarColor">@color/mcgpalette0_900</item>
        <item name="android:textColorSecondary">@color/mcgpalette0_900</item>
        <item name="android:textColorSecondaryInverse">@color/mcgpalette0_900</item>
        <item name="android:textColorPrimaryInverse">@color/mcgpalette0_900</item>
        <item name="android:windowBackground">@color/mcgpalette0_900</item>
    </style>

</resources>
